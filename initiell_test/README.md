brew install terraform

brew install azure-cli
az login

Choose subscription. 

python3 -m venv venv
source venv/bin/activate
pip install openai

terraform init
terraform plan
terraform apply
terraform destroy
